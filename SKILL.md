---
name: goal-skill
description: "通过逐轮澄清或项目查证，将模糊任务整理为可验收计划，再按授权启动 Codex Goal。用于先完善计划再执行 Goal；仅解释 Goal 或执行已清晰的普通任务时不适用。"
license: MIT
---

# Goal 规划向导

帮助用户逐步确定想要的结果、可接受的边界和完成证据，形成简洁计划，再交给 Codex Goal 持续执行。默认用用户的语言交流；先复用已有对话、项目规范和授权。

## 选择协作方式

根据用户表达直接选择，不必先让用户填模式问卷。两种方式可以随时混用或切换。

- **陪伴澄清**：用户想自己思考、慢慢完善，或偏好仍不明确。每轮围绕一个重要决策，引导用户举实例、比较结果；把回答转成计划中的具体内容，保留用户决策权。
- **项目辅助**：用户希望结合代码、现状或已有材料补全。先做范围有限的只读检查，提出有证据的草案；只把项目无法回答的偏好、取舍和范围问题交给用户。
- 未指明方式时，场景明确且确实涉及当前项目，才先辅助查证；场景还不明确，先问一个关键问题。当前工作目录不自动代表用户的任务范围，非代码任务按其材料和产物规划。
- 需求和验收已经清楚时直接形成执行草案，不强制经历多轮提问。用户只要计划时，交付计划即止。

## 1. 建立最小共识

若用户要求暂停当前 Goal 再重新规划，先读取 [references/goal-handoff.md](references/goal-handoff.md)，按当前能力处理暂停请求；不能操作时说明需要的客户端动作，不声称已暂停。之后再继续澄清。

用一两句话复述当前理解，识别本次请求是调查、解释、设计还是实施。区分以下内容，避免把推测写成既定需求：

- 用户已经确定的结果、约束和授权。
- 项目或材料已经证实的事实，附简短位置或证据。
- 可以公开说明后采用的低风险、可逆假设。
- 会改变验收、方案或操作范围的待决问题。

当前目标过大时，提出一个可独立验收的最小阶段及其价值，请用户决定是否采用。保留原始意图；不要自行把完整功能缩成演示，或把修复改成仅提交调查报告。

## 2. 逐轮补全，而非一次盘问

通常每轮只问一个最影响下一步的决策；只有紧密相关且容易回答时才合并。优先顺序随信息缺口调整：

1. 具体场景：谁在什么情况下遇到什么问题？
2. 期望结果：完成后有什么可观察变化？
3. 完成证据：用什么实例、测试、指标或产物确认结果？
4. 边界和取舍：本次做到哪里，哪些兼容性或行为必须保留？

先复用已有答案，再处理信息缺口：项目事实查证；低风险、可逆的普通实现细节说明假设后自行决定；影响价值、范围或兼容性的取舍问用户；不影响当前阶段的细节延后。

- 让问题具体。例如“最希望优先改善哪一步操作？”比“请完善需求”更容易回答。
- 用户说“不知道”时，提供两三个具体方案、简短取舍和有理由的推荐。推荐仍是建议，不能当成用户已经接受。
- 避免诱导用户接受不必要的复杂方案。用户想自己思考时，先帮助其描述期望结果，再讨论技术做法。
- 有可用提问工具时按该工具约定使用；异步等待期间仅推进不依赖答案的检查。没有合适工具时，在正常回复中问一个简短问题并等待。
- 沉默、超时或预选选项不代表同意；实质性待决问题保留等待。

每轮只更新发生变化的共识，并用一句话说明回答怎样改变计划。例如：“你选择保留旧配置，因此把升级后无需改配置加入验收。”用户改变想法时只修改受影响部分；要求暂停、仅保留草案或切换方式时按其要求收束。

## 3. 用项目证据补全可执行性

项目辅助方式下，按当前问题选取最少必要材料：

- 检查适用的 `AGENTS.md`、当前分支和工作区状态；只查看与任务重叠的已有改动。
- 从相关入口、直接调用链、已有测试、构建清单和约定定位实现边界。先查文件路径和符号，再读取相关内容，避免全仓扫描。
- 找到真实的验证命令、运行环境和产物位置。未验证可运行的命令标注“待验证”，不能把推测当已通过结果。
- 区分环境限制与产品决策，例如缺少 Windows 环境或测试数据。先确认是否有安全替代证据，再判断是否影响目标可达成性。

已找到相关入口、主要约束和可行验证方式，足以支持草案时，结束本轮探索；未影响方案选择的实现细节留到执行阶段。若必要信息仍缺失，明确缺口并选择下一次有针对性的查证。

此阶段以阅读和安全诊断为主；不要提前实现功能、安装依赖、迁移数据或部署。必要检查应符合用户授权和项目规则。若无法确定目标项目，先定位用户指的是哪一个。

项目事实用于约束技术方案，不能代替用户决定产品取舍。缺少源码时可以继续完善需求和材料清单；若实现依赖这些材料，执行准备仍不充分。

## 4. 形成可审阅计划

当继续提问已不会显著改变范围或成功标准时，给出一份紧凑草案。通常包含两到五条验收条件和少量执行步骤，按任务复杂度缩放。

```text
目标：完成后应成立的具体事实。
验收：可观察行为 + 对应的测试、命令、指标或产物。
范围与约束：本次修改边界、兼容性要求，以及适用项目规范。
依据与假设：关键证据位置、公开采用的假设、仍待验证的条件。
执行：
1. 步骤 -> 验证：检查方式。
2. 步骤 -> 验证：检查方式。
迭代与阻塞：下一步如何依据证据选择；什么输入缺失会阻止继续。
预算：仅在用户明确指定时列出 token 上限。
交付：最终需要的文件、验证结果和简短说明。
```

不要为填模板制造内容。已有 `AGENTS.md` 的通用规则引用即可；不要重复整份规范。默认在对话中维护草案，用户要求保存或项目流程要求时才写入既有计划位置。

进入执行前检查：

- 成功条件可判断，证据足以覆盖正确性及适用的兼容性要求。
- 关键偏好和范围已确定；剩余未知项可以在授权范围内通过执行解决。
- 有合理的第一步及验证路径；没有把必须由用户决定的取舍藏在假设里。
- 已展示具体计划，并具备对该范围启动 Goal 的授权。

若还缺关键决策，只问那个决策。准备充分且尚无启动授权时，简短询问是否按展示的计划启动 Goal；对这个明确问题的肯定回复即可。单独调用本 skill、称赞方案或回答某个澄清问题不自动授权启动。

尊重已有授权：用户明确说“你补全计划后直接执行”等，可在上述条件满足后展示计划并启动，不再要求重复确认。用户确认了同一份计划并明确要求启动，也直接继续。重大范围变化需重新获得对应授权；普通实现细节在既有授权内自行处理。

## 5. 交接并执行 Goal

仅在准备启动、继续、调整或停止真实 Goal 时，读取 [references/goal-handoff.md](references/goal-handoff.md)，按当前环境的 Goal 工具契约操作。没有现有 Goal 需要处理的普通澄清无需读取。

目标执行开始后，围绕已确认计划推进；仅在新的证据影响验收或范围时回到澄清。不要在每个实现步骤重新要求用户批准。

## Token 效率

- 不为填满模板延长采访；用户主动选择慢慢思考时尊重其节奏。
- 复用仍有效的项目事实；文件、环境或需求变化后再校验相关部分。跨上下文延续时保留一份简短的当前计划与证据摘要。
- 不承诺固定节省比例。执行阶段的检查复用、预算和停止规则见交接参考。

## 维护评测

仅在维护或评测本 skill 时使用 [evals/cases.yaml](evals/cases.yaml)。普通引导不加载案例；案例是待运行的行为规格，格式校验或人工走查不能替代真实执行结果。
